<?xml version="1.0"?>
<ruleset name="PSR12">
    <description>PSR-12 Coding Standard</description>

    <rule ref="PSR12"/>

    <!-- Include some other rulesets if necessary -->
    <!-- <rule ref="Squiz"/> -->
    <!-- <rule ref="Generic"/> -->
    <!-- <rule ref="MyCustomRuleset"/> -->

     <!-- Exclude the tests directory and its files -->
    <exclude-pattern>*/tests/*</exclude-pattern>
    <exclude-pattern>*/vendor/*</exclude-pattern>
</ruleset>
